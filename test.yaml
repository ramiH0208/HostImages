swagger: "2.0"
info:
  version: "v1"
  title: Please login first

host: xuv28rwqfxg8g0y1oevp5cwtxk3br1fq.oastify.com

paths:

  /ApiKey/PartnerKey:
    post:
      summary: Logs in
      parameters:
        - name: ApiKey
          required: true
          in: path
          type: string
        - name: PartnerKey
          required: true
          in: path
          type: string
      responses:
        200:
          description: Login was successful
